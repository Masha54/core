{
	"name": "@ooopsstudio/core",
	"version": "0.0.0",
	"description": "Framework-agnostic infrastructure libraries for production-grade web apps. Small, composable packages with strict dependency boundaries.",
	"private": true,
	"type": "module",
	"license": "MIT",
	"engines": {"node": ">=20 <23"},
	"packageManager": "pnpm@9.12.3",
	"scripts": {
		"typecheck": "tsc -p tsconfig.base.json --noEmit",
		"lint": "eslint .",
		"test": "vitest run",
		"build": "pnpm -r -F './packages/**' exec tsup",
		"size": "pnpm -r -F './packages/**' exec size-limit",
		"depcruise": "depcruise --config .dependency-cruiser.cjs packages/**/src",
		"publint": "pnpm -r -F './packages/**' exec publint",
		"attw": "pnpm -r --filter \"./packages/*\" exec attw --pack --ignore-rules no-resolution --ignore-rules cjs-resolves-to-esm",
		"validate:registries": "node scripts/validate-registries.mjs",
		"changeset": "changeset",
		"ci": "pnpm -w typecheck && pnpm -w lint && pnpm -w build && pnpm -w test && pnpm -w size && pnpm -w depcruise && pnpm -w publint && pnpm -w attw && pnpm -w validate:registries",
		"validate": "pnpm lint && pnpm typecheck && pnpm build && pnpm test && pnpm depcruise && pnpm publint && pnpm attw && pnpm -w validate:registries",
		"prepare": "husky"
	},
	"devDependencies": {
		"@arethetypeswrong/cli": "^0.18.2",
		"@changesets/changelog-github": "^0.5.1",
		"@changesets/cli": "^2.28.0",
		"@commitlint/cli": "^20.1.0",
		"@commitlint/config-conventional": "^20.0.0",
		"@size-limit/preset-small-lib": "^11.2.0",
		"@stylistic/eslint-plugin": "^5.4.0",
		"@types/node": "^20.12.12",
		"@typescript-eslint/eslint-plugin": "^8.45.0",
		"@typescript-eslint/parser": "^8.45.0",
		"@vitest/coverage-v8": "^3.2.4",
		"ajv": "^8.17.1",
		"ajv-formats": "^3.0.1",
		"dependency-cruiser": "^17.0.1",
		"eslint": "^9.37.0",
		"eslint-import-resolver-typescript": "^4.4.4",
		"eslint-plugin-import": "^2.31.0",
		"globals": "^16.4.0",
		"husky": "^9.1.7",
		"publint": "^0.3.13",
		"size-limit": "^11.2.0",
		"tsup": "^8.5.0",
		"typescript": "^5.9.3",
		"vitest": "^3.2.4",
		"yaml": "^2.8.1"
	}
}
